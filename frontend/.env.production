# Loaded automatically by `next build` (including Docker). Safe to commit — NEXT_PUBLIC_* is exposed to the browser.
# Change this if your API host changes.
NEXT_PUBLIC_API_URL=https://api.utkalautomobiles.co.in/api
